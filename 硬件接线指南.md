# 硬件接线指南

## ESP32-C5 开发板选型

推荐使用以下任一开发板：
- **ESP32-C5-DevKitM-1**（乐鑫官方，推荐）
- **微雪 ESP32-C5-WROOM 开发板**
- **淘宝第三方 ESP32-C5 核心板**（¥16-28）

## LCD 屏幕接线

使用 ST7789 240x320 TFT（SPI接口），常见模块：
- LILYGO T-Display 1.9寸/2.0寸 ST7789
- 淘宝通用 2.0寸 ST7789 SPI模块（¥8-15）

### SPI 引脚分配（默认配置）

| ST7789 引脚 | ESP32-C5 GPIO | 说明 |
|:-----------:|:------------:|:----:|
| SCLK | GPIO1 | SPI 时钟 |
| MOSI (SDA) | GPIO3 | SPI 数据 |
| CS | GPIO5 | 片选 |
| DC (RS) | GPIO4 | 数据/命令切换 |
| RST | GPIO6 | 复位 |
| BL (LED) | GPIO7 | 背光控制 |
| MISO | 不接 | 只写不读 |
| VCC | 3.3V | 电源 |
| GND | GND | 地线 |

### 修改引脚

如果开发板引脚不同，编辑 `components/lcd_display/include/lcd_display.h`：

```c
#define LCD_PIN_SCLK    1   // 改成你的 SCLK 引脚
#define LCD_PIN_MOSI    3   // 改成你的 MOSI 引脚
#define LCD_PIN_CS      5   // 改成你的 CS 引脚
#define LCD_PIN_DC      4   // 改成你的 DC 引脚
#define LCD_PIN_RST     6   // 改成你的 RST 引脚
#define LCD_PIN_BK      7   // 改成你的背光引脚（-1=常亮）
```

## 按键接线

3个轻触按键，一端接GPIO，一端接GND（内部上拉，不需要外部电阻）。

| 按键功能 | ESP32-C5 GPIO | 作用 |
|:--------:|:------------:|:----:|
| PREV | GPIO0 | 上一项 / 返回上级页面 |
| NEXT | GPIO8 | 下一项 / 向下滚动 |
| SEL  | GPIO9 | 确认 / 进入详情页 |

### 修改按键引脚

编辑 `main_rx/app_main.c`：

```c
#define BTN_PREV_GPIO   0   // 改成你的按键引脚
#define BTN_NEXT_GPIO   8   // 改成你的按键引脚
#define BTN_SEL_GPIO    9   // 改成你的按键引脚
```

## 接线示意图

```
ESP32-C5-DevKitM-1
┌──────────────────────────┐
│                          │
│  3.3V ─────── VCC (LCD)  │
│  GND  ─────── GND (LCD)  │
│  GPIO1 ───── SCLK (LCD)  │
│  GPIO3 ───── MOSI (LCD)  │
│  GPIO4 ───── DC   (LCD)  │
│  GPIO5 ───── CS   (LCD)  │
│  GPIO6 ───── RST  (LCD)  │
│  GPIO7 ───── BL   (LCD)  │
│                          │
│  GPIO0 ───── BTN (PREV) ─── GND │
│  GPIO8 ───── BTN (NEXT) ─── GND │
│  GPIO9 ───── BTN (SEL)  ─── GND │
│                          │
└──────────────────────────┘
```

## 注意事项

1. **电源**：ST7789 屏幕功耗约 50-80mA，ESP32-C5 约 240mA，总计约 300mA。如果用 USB 供电没问题；用电池建议 500mAh 以上。

2. **SPI 速率**：默认 40MHz，如果显示异常可降到 20MHz（修改 `LCD_SPI_FREQ_HZ`）。

3. **PSRAM**：LCD 帧缓冲需要 150KB 内存。ESP32-C5 如果没有 PSRAM，会自动用 DMA 内存。推荐选带 PSRAM 的模组（ESP32-C5-N4R2 或 N8R8）。

4. **背光**：如果不接背光控制引脚，把 `LCD_PIN_BK` 改为 `-1`。

5. **屏幕方向**：如果屏幕显示方向不对，在 `lcd_display.c` 中调整：
   ```c
   esp_lcd_panel_swap_xy(s_panel, true);   // 旋转90度
   esp_lcd_panel_mirror(s_panel, true, false); // 镜像
   ```
