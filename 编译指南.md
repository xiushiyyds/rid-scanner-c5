# 编译指南 — NekolunaRIDScanner 自定义版

## 环境要求

- **ESP-IDF**: v5.4 或更高（需支持 ESP32-C5）
- **操作系统**: Windows 10/11、Linux、macOS
- **Python**: 3.8+
- **CMake**: 3.16+

## 一、安装 ESP-IDF（Windows）

### 方法1：ESP-IDF 离线安装包（推荐）

1. 下载 ESP-IDF Windows 离线安装包：
   https://dl.espressif.com/dl/esp-idf/

2. 运行安装程序，选择 ESP-IDF **v5.4** 或更高版本
3. 安装完成后，打开 "ESP-IDF CMD" 或 "ESP-IDF PowerShell"

### 方法2：Git + pip 手动安装

```powershell
# 1. 安装 Git for Windows（如果没有）
# https://git-scm.com/download/win

# 2. 克隆 ESP-IDF
mkdir C:\esp
cd C:\esp
git clone -b v5.4 --recursive https://github.com/espressif/esp-idf.git
cd esp-idf

# 3. 运行安装脚本
install.bat esp32c5

# 4. 设置环境变量（每次开新终端都要执行）
export.bat
```

## 二、编译固件

```powershell
# 1. 进入项目目录
cd NekolunaRIDScanner_custom

# 2. 设置目标芯片
idf.py set-target esp32c5

# 3. 配置（可选，检查默认配置）
idf.py menuconfig
```

### menuconfig 关键配置项

如果使用带 PSRAM 的 ESP32-C5 模组（推荐）：
```
Component config → ESP PSRAM → [*] Support for external, SPI-connected RAM
```

如果不需要 USB CDC 输出（仅串口）：
```
Component config → ESP System Settings → Channel for console output → UART
```

### 编译

```powershell
# 4. 编译
idf.py build
```

编译成功后，固件在：
```
build/remoteid_scanner.bin
```

## 三、烧录固件

### 方法1：idf.py 一键烧录

```powershell
# 连接 ESP32-C5 USB，确认 COM 端口
idf.py -p COM3 flash
```

### 方法2：使用 flash_download_tool（原作者推荐）

项目 Releases 页面提供了 `flash_download_tool.zip`：

1. 解压 flash_download_tool
2. 打开 `flash_download_tool_xxx.exe`
3. 选择芯片：ESP32-C5
4. 添加固件文件：`build/remoteid_scanner.bin`
5. 地址填：`0x0`
6. 选择 COM 口和波特率（921600）
7. 点击 START

### 方法3：使用预编译固件（原版）

如果你只想用原版功能（不需要LCD增强），可以直接用作者提供的预编译固件：
```
NekolunaRIDScanner_esp32c5.zip（从 Releases 下载）
```
用 flash_download_tool 烧录即可。

## 四、验证

烧录完成后：

1. 打开串口终端（115200 波特率）
2. 复位 ESP32-C5
3. 应该看到启动日志：
```
I (xxx) LCD: Initializing LCD display (ST7789 240x320)
I (xxx) LCD: Framebuffer allocated: 153600 bytes
I (xxx) LCD: LCD display initialized successfully
I (xxx) RID_MAIN: LCD display ready (ST7789 240x320)
```

4. LCD 屏幕应显示主页：
   - 标题栏（绿色）：RID SCANNER v2.0
   - UAVS: 0 ACTIVE
   - CH: 6, RSSI: ---
   - PROTOCOLS 分布

## 五、常见问题

### Q: 编译报错 "unsupported target esp32c5"
A: ESP-IDF 版本太低。请升级到 v5.4+。

### Q: LCD 屏幕白屏/黑屏
A: 检查接线，特别是 DC 和 RST 引脚。在 `lcd_display.c` 中尝试调整：
```c
esp_lcd_panel_invert_color(s_panel, false);  // 改 invert
```

### Q: 内存不足（PSRAM 未启用）
A: 帧缓冲需要 150KB。如果 ESP32-C5 没有 PSRAM：
- 使用更小的屏幕（如 128x160，只需 40KB）
- 或者在 `lcd_display.c` 中改为逐行刷新模式

### Q: 按键无响应
A: 检查按键接线是否接GND。GPIO 内部上拉，按下时应该是低电平（下降沿触发）。

### Q: 编译报错找不到 lcd_display.h
A: 确认 `components/lcd_display/include/` 目录下有 `lcd_display.h`，且 `components/lcd_display/CMakeLists.txt` 中 `INCLUDE_DIRS` 设置为 `"include"`。

## 六、后续开发方向

### 第二期：增强检测
- 增加 BLE RID 扫描（当前仅 WiFi）
- 多信道轮询扫描
- Friend or Foe 项目 BLE 扫描逻辑移植

### 第三期：DJI DroneID 逆向
- 搭建真实 DJI 无人机抓包环境
- 分析 DJI 私有 Wi-Fi 帧格式
- 编写 DJI DroneID 解析器（需真实抓包数据）

### 第四期：定位与追踪
- GPS 模块集成（计算无人机距离和方位）
- 地图显示（通过 BLE 连接手机，用 App 显示地图）
- 多设备组网（WiFi Mesh / ESP-NOW）
